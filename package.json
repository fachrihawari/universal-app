{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "clean": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "reset": "yarn clean && yarn bootstrap",
    "run-web": "lerna run start --scope @app/web",
    "run-mobile": "lerna run start --scope @app/mobile",
    "run-desktop": "lerna run start --scope @app/desktop"
  }
}
